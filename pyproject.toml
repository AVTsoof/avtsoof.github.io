[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "avtsoof"
version = "0.1.0"
description = "Shared toolkit for the avtsoof.github.io blog"
requires-python = ">=3.9"

[tool.setuptools]
packages = ["avtsoof"]
