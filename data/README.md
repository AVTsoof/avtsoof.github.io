# data/ — root datastore (git-ignored)

Raw and large datasets live here, reached from post code via
`avtsoof.common_utils.data_dir("<name>")`. Everything under this folder is
git-ignored except this README and `.gitkeep`; never commit raw data under
`docs/`.
